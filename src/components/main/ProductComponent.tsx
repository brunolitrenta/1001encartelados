import styles from './ProductComponent.module.scss'

export function ProductComponent() {
    return (
        <section>
            <h1 className={styles.title}>Pregos</h1>
        </section>
    )
}